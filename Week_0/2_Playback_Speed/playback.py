# Prompt the user to input something and store it in the variable 'input'.
input_text = input(str("Please type something. Yes, anything! : "))

# Replace all spaces in the input with '...' and store the result in the variable 'playback'.
playback = input_text.replace(" ", "...")

# Print the modified input text.
print(playback)
